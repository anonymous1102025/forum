# URL of your Render backend — no trailing slash
# Local dev: leave empty (Vite proxy handles /api/* -> localhost:8000)
# Production: set to your Render service URL, e.g. https://forum-analytics-api.onrender.com
VITE_API_URL=
